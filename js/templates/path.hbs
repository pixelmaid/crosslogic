var {{id}} = new Path({
    segments: [{{#each segments}}{{#if @last}}[{{this}}]{{else}}[{{this}}],{{/if}}{{/each}}],
    strokeColor: {{strokeColor}},
    fillColor: {{fillColor}},
    closed: {{closed}}
});